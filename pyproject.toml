import uvicorn
from app import app

def main():
    uvicorn.run(app, host="0.0.0.0", port=7860)

if __name__ == "__main__":
    main()
```

**Fix 3 — Replace `uv.lock`** with this content:
```
version = 1
requires-python = ">=3.10"

[[package]]
name = "email-triage-openenv"
version = "1.0.0"
source = { editable = "." }
dependencies = [
  { name = "openenv-core" },
  { name = "fastapi" },
  { name = "uvicorn" },
  { name = "pydantic" },
]
